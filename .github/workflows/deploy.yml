# Este workflow fue reemplazado por Vercel (deploy automático en cada push a main).
# Se mantiene el archivo para evitar errores de referencias, pero está desactivado.
name: Deploy (Disabled - using Vercel)

on:
  workflow_dispatch: # Solo se ejecuta si se dispara manualmente, nunca en push

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Informar
        run: echo "Deploy manejado por Vercel. Este workflow está inactivo."
